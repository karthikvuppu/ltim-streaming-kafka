#TODO: is this used? Does not seem to be mounted anywhere.
apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: mds-client-plain
  namespace: confluent
spec:
  refreshInterval: "1m"  # May be set to zero to fetch and create it once
  secretStoreRef:
    name: global-secret-store
    kind: ClusterSecretStore
  target:
    name: mds-client-plain
    creationPolicy: Owner
  data:
    - secretKey: plain.txt
      remoteRef:
        key: {{ .Values.environment }}/{{ .Values.clusterType}}/mds-client-plain
        property: plain.txt