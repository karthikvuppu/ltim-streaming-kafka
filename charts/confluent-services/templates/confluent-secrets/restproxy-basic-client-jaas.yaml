apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: restproxy-basic-client-jaas
  namespace: confluent
spec:
  refreshInterval: "1m"  # May be set to zero to fetch and create it once
  secretStoreRef:
    name: global-secret-store
    kind: ClusterSecretStore
  target:
    name: restproxy-basic-client-jaas
    creationPolicy: Owner
  data:
    - secretKey: client.jaas
      remoteRef:
        key: {{ .Values.environment }}/{{ .Values.clusterType}}/restproxy-basic-client-jaas
        property: client.jaas