apiVersion: kafka.strimzi.io/v1beta2
kind: KafkaTopic
metadata:
  name: platform.e2eclaude.created
  namespace: kafka
  labels:
    strimzi.io/cluster: my-kafka
  annotations:
    requested-by: claude-e2e-test
    description: 'End-to-end test topic — verify webhook → ArgoCD → Strimzi pipeline'
spec:
  partitions: 1
  replicas: 1
  config:
    retention.ms: 172800000
    cleanup.policy: delete
    min.insync.replicas: '1'
