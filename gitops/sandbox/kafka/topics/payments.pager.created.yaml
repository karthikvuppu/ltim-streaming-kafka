apiVersion: kafka.strimzi.io/v1beta2
kind: KafkaTopic
metadata:
  name: payments.pager.created
  namespace: kafka
  labels:
    strimzi.io/cluster: my-kafka
  annotations:
    requested-by: v.karthik@ltimindtree.com
    description: 'creating topic for engg use '
spec:
  partitions: 3
  replicas: 1
  config:
    retention.ms: 172800000
    cleanup.policy: delete
    min.insync.replicas: '1'
